<div>
  <h1>My lists</h1>
  <ul>
    <% @lists.each do |list| %>
    <li><%= link_to "#{list.name}", list %></li>
    <% end %>
  </ul>
</div>
<div>
  <%= link_to "Create a new list", new_list_path %>
</div>
